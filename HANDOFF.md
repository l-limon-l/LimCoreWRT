# LimCoreWRT — состояние работ

Документ для продолжения в новом чате. Актуален на **13 августа 2026**.

---

## 1. Доступ к роутеру

```bash
ssh root@192.168.1.1
```

Вход по SSH-ключу, пароль вводить не нужно (пароль есть в переписке первого чата, сюда намеренно не вынесен — файл лежит в репозитории).

Полезное на устройстве:

| Что | Где |
|---|---|
| Настройки | `uci show limcore` |
| Сгенерированный конфиг ядра | `/var/run/limcore/core.json` |
| Лог ядра | `/var/run/limcore/core.log` (+ `.log.1`) |
| Лог наблюдения за связью | `/var/run/limcore/health.log` |
| Управление службой | `/etc/init.d/limcore {start,stop,restart,status}` |
| Скрипты | `/etc/limcore/scripts/`, `/usr/share/limcore/scripts/` |
| Бэкенд интерфейса | `/usr/share/rpcd/ucode/luci.limcore` |

Железо: OpenWrt 25.12.5, aarch64_cortex-a53, apk-tools 3.0.5, overlay ubifs 95.6 МБ (занято 41%).

**Важное ограничение по проверкам.** На компьютере включён VPN, поэтому все проверки ведутся с роутера. Проверки через ПК — крайний вариант и только когда вы на месте и можете обеспечить условия.

---

## 2. Что установлено сейчас

```
luci-app-limcore-2026.08.12-r37
luci-i18n-limcore-ru-2026.08.12-r37
luci-theme-limcore-1.0.1-r20260813
sing-box-extended-1.13.18.2.6.5-r0
```

Текущие настройки:

| Параметр | Значение |
|---|---|
| Узел | 🇫🇮 Финляндия (`cf.mtrx.win`, ~75 мс — самый быстрый в подписке) |
| Режим прокси | `redirect_tproxy` |
| Маршрутизация | `proxy_banned_ru` (проксировать заблокированное) |
| WARP | **удалён из проекта 13 августа**, см. раздел 4 |
| Автообновление приложения | включено, раз в 3 дня в 04:00 |
| Наблюдатель связи | **выключен** (`health_log=0`) — включается на вкладке диагностики |

Служба запущена, два экземпляра procd: ядро и чистильщик логов. Третий,
наблюдатель связи, появляется только при `health_log=1`.

---

## 3. Что было сделано

### Переименование и чистка
- Всё переименовано в **LimCore**, включая пути (`homeproxy` убран отовсюду).
- **hiddify-core удалён полностью** — и пакет, и все ветки разбора его формата в генераторе конфига. Остался только sing-box-extended (форк shtorm-7).
- Удалены репозитории passwall, дававшие предупреждения `UNTRUSTED signature`.

### Починка установки ядра
Симптом был: установщик писал «singbox установлен», а интерфейс показывал «✗ не найдено».

Причина — обрыв загрузки: пакет регистрировался в apk, а бинарник до диска не доезжал. Исправлено в `core_mgmt.uc`: проверка размера скачанного файла, проверка наличия `/usr/bin/sing-box` после установки, откат при неудаче, показ настоящей ошибки apk вместо молчания.

### Починка самообновления
Кнопка «Обновить LimCore» не работала по двум независимым причинам:
1. rpcd убивал дочерний процесс — вылечено запуском через `setsid`;
2. `/usr/bin/limcore-update` приезжал с правами `100644`, то есть не исполнялся — выставлен бит запуска и вызов через `sh`.

Заодно: проверка «жив ли процесс» переведена с `pgrep -f` (он находил сам себя и всегда возвращал «работает») на PID-файл.

### Подписывание пакетов
Настроено сквозное подписывание APK: ключ в секретах GitHub, `apk mkpkg --sign-key` в сборке. Предупреждений о неподписанных пакетах больше нет.

> Уточнение к более раннему объяснению: apk сопоставляет ключи **по содержимому, а не по имени файла**. Переименование ключа было безобидным, но обоснование, которое я тогда привёл, было неверным.

### Discord
Голос и демонстрация экрана заработали после добавления диапазонов **19294–19344** и **50000–50100** — в правило маршрутизации и в список перехвата одновременно. До этого в звонке был пинг 5000.

### Cloudflare WARP
Был добавлен как эндпоинт `warp` поверх основного прокси. **Удалён 13 августа целиком** — разбор в разделе 4.

### Проверка обновлений
- Кнопка «Проверить обновление» с показом «установлено / доступно».
- Проверка расширена на **все** компоненты: sing-box, ByeDPI, Zapret.
- Результат кэшируется на 15 минут в `/tmp/limcore-update-check.json`, на каждый запрос к GitHub одна повторная попытка (сам `api.github.com` идёт через прокси и иногда отвечает ошибкой или упирается в лимит запросов).
- Добавлена возможность автоматической проверки по расписанию через cron с метками `#limcore_*`.

### Прочее
- Конфиг проверяется через `sing-box check` **до** запуска — битый конфиг больше не роняет службу.
- Логи ротируются, а не обрезаются, и живут достаточно долго, чтобы их можно было прочитать.
- Исправлено размножение ip-правила tproxy при каждом перезапуске.
- Установщик берёт `/releases/latest` вместо первого попавшегося релиза (раньше мог поставить версию старее установленной).

---

## 4. Cloudflare WARP — удалён 13 августа

WARP решал одну конкретную задачу: IP основного узла (Финляндия, Hetzner) помечен
у Google как VPN. С этого адреса не работает Gemini и нет рекламы на YouTube —
то есть адрес узнаётся и ограничивается. Дело не в стране, а именно в адресе.

Устройство было такое: эндпоинт `warp` в sing-box с `detour` на основной прокси,
то есть поверх VPN, а не вместо него. Два режима — весь проксируемый трафик либо
только перечисленные домены.

**Почему удалён.** Вечером 13 августа Gemini на компьютере встал: блокировка по
региону, ошибка 1060 при отправке, бесконечная загрузка истории чатов. Разбор дал
следующее.

- Через WARP шли только перечисленные домены, а запросы с подписью сессии Google
  (`play.google.com/log` с `SAPISIDHASH`, `accounts.google.com`) уходили с
  помеченного адреса. Google видел один сеанс с двух адресов сразу.
- Добавление доменов по одному давало временное улучшение и снова срывалось:
  приложение Google обращается к десяткам хостов, достаточно одного забытого.
  За вечер список вырос с 8 до 15 записей и всё равно не сошёлся.
- Объективный показатель нашёлся в телеметрии самого Gemini: в запросах к Google
  Analytics поле `ep.user_onboarding_status` принимает значения `available` либо
  `location_rejected`. По нему видно состояние без догадок — рекомендую начинать
  с него, если тема всплывёт снова.
- Ограничение конструкции: у эндпоинта стоял `detour: main-out`, то есть WARP
  подключался к Cloudflare **через** основной узел, и Cloudflare выдавал ближайший
  к нему выход. Измерено: `104.28.222.16`, colo HEL, `loc=FI`. Сменить страну через
  WARP в такой схеме нельзя — меняется только адрес внутри той же страны.

Итог: результат нестабильный, поддержание списка доменов — гонка без конца.
Владелец решил убрать целиком.

**Если понадобится вернуть** — код в истории, до коммита «Cut WARP out entirely».
Но возвращать стоит не как было: либо весь трафик Google одним правилом с явными
исключениями для тяжёлых хостов (`www.google.com`, YouTube, загрузки Play), либо
вовсе не через WARP, а сменой узла на такой, чей адрес Google не метит. Поимённый
список доменов — путь, который уже не сработал.

---

## 5. Где мы остановились: нестабильность связи

Ваша жалоба: соединение подвисает, «грузится и оп, остановка», в Telegram обрывались голосовые.

Поставлен постоянный наблюдатель `health_probe.sh` (замер раз в 30 с): задержка до узла через Clash API, пинг до шлюза провайдера, время локального DNS-запроса. При промахе делается **повторная проверка через 2 секунды**, и `DOWN` записывается только если провалились обе — иначе журнал заполнялся призрачными обрывами.

Накоплено 1581 замеров:

| Показатель | Значение |
|---|---|
| Подтверждённые обрывы узла | **71 (4.5%)** |
| Промахов, вылечившихся 2-й попыткой | 122 |
| Сбоев канала до провайдера | **0** |
| Сбоев DNS | 1 |
| Задержка узла | мин 64 / медиана 72 / p90 90 / макс 415 мс |

**Вывод: провайдер и DNS чисты, обрывается именно узел.** Ранняя оценка в 5.5% была завышена — почти две трети промахов оказались шумом измерения. Но 71 подтверждённый обрыв — настоящие, и по времени они совпадают с тем, что вы описывали.

Отдельная находка: при параллельном сравнении 11 узлов **whitelist 1 и whitelist 2 мертвы полностью** (100% отказов), whitelist 3 при этом жив (199 мс). Стоит спросить владельца подписки. Разброс задержек по узлам большой: от 76–90 мс у лучших до 429–489 мс у худших. Финляндия — из быстрых.

### Чем это кончилось — 13 августа

Прогон на исправленной сборке, 1015 замеров за 20 часов, узел Финляндия:

| Показатель | 11 августа | 13 августа |
|---|---|---|
| Подтверждённые обрывы узла | 71 из 1581 (**4.5%**) | **2 из 1015** — обе в момент загрузки роутера, до настройки |
| Промахи, вылеченные повтором | 122 | 19 |
| Задержка узла | мин 64 / медиана 72 / p90 90 / макс 415 | мин 64 / медиана 81 / p90 116 / макс 3030 |
| Сбои DNS | 1 | 5 |

Обрывов при работе — ноль. Одиночный всплеск до 3 секунд остался, медиана чуть
выросла (81 против 72), но хвост p90 и доля отказов упали.

**Утечки памяти нет.** rss ядра по часам за пять часов: потолок 89, 89, 90, 90,
85 МБ — не растёт. Пол ходит между 45 и 73 в такт сборке мусора и перезапускам,
и по двум соседним окнам его легко принять за дрейф — смотреть надо на потолок.

Замер скорости в наблюдателе по-прежнему **не сделан** (см. незакрытые задачи).

---

## 6. Незакрытые задачи

| № | Задача |
|---|---|
| 0 | **Zapret/ByeDPI без ядра.** Установщик теперь спрашивает, ставить ли ядро, но отказ оставляет систему нерабочей целиком: оба обхода DPI — это outbound'ы sing-box (`zapret-out` ставит `routing_mark`, `byedpi-out` ведёт в SOCKS на 5335), трафик до них доводит ядро. Чтобы «только запрет» стал правдой, нужен свой путь разметки на nft, мимо sing-box. Это функция, а не заплатка |
| 0b | С телефона (192.168.1.213) шли соединения на `194.221.250.50:443` с именем `www.google.com` в SNI — адрес недоступен, 148 таймаутов в логе ядра, потом прекратилось само. **DNS ни при чём:** и наш резолвер, и `77.88.8.8` отдают на этот домен только `142.251.x.x`, а приватный DNS на телефоне не настроен (проверено владельцем). То есть адрес зашит в каком-то приложении, а имя Google подставлено в SNI. Определить приложение можно только поймав момент: `grep 194.221.250 /proc/net/nf_conntrack` даст порт источника. Пока не воспроизводится — на работу не влияет |
| 1 | Проверка целостности загрузок для **ByeDPI и Zapret** — сделано только для sing-box, у остальных та же дыра, что давала «установлено, но не найдено» |
| 2 | Ограничить поля протокола по `node.type` в `generate_client.uc` — сейчас показываются поля, неприменимые к выбранному типу |
| 3 | Убрать косметический шум `service delete (Not found)` в `update_subscriptions.uc` |
| 4 | Сплошной аудит `client.js`, `node.js`, `update_subscriptions.uc` — до них подробная вычитка не дошла |
| 5 | Решить по замеру скорости в наблюдателе (см. раздел 5) |
| 6 | `antigravity-hub-auto-updater-…run.app` по-прежнему идёт напрямую с российского адреса. На работу не влияет, но автообновление Antigravity может вести себя странно — при случае проверить |
| 7 | `po/templates/limcore.pot` устарел (785 строк против 1029 в исходниках) — при следующем прогоне xgettext пересобрать. На сборку не влияет: пакет собирается из `po/ru/limcore.po` |
| 8 | В `po/ru/limcore.po` 12 повторяющихся `msgid` (были и раньше). `po2lmo` их проглатывает, `msgfmt` бы отказался — почистить |

---

## 7. Грабли, на которые не стоит наступать снова

- **`[ -n ${IPKG_INSTROOT} ]`** без кавычек всегда истинно — из-за этого тело postinst не выполнялось. Кавычки обязательны.
- **`access('/proc/' + '')` возвращает истину** — при проверке PID нужна защита от пустой строки.
- **`regexp()` в ucode с `\S`** молча не находит ничего. Использовать разбиение по пробелам.
- **ubifs сообщает о свободном месте пессимистично** сразу после записи (22 МБ против реальных 54 МБ). Не пугаться и не менять пороги по горячим следам.
- **Сжатие overlay — 2.21x**, считается как `du` против `df`, а не `du` против `du`. Пороги в `core_mgmt.uc` (`FULL_OVERLAY_RAW_KB=81920`, `FULL_OVERLAY_COMP_KB=32768`) верны, я их однажды сломал неверным замером и вернул обратно.
- **Одиночные кавычки внутри `echo -e '...'` в CI** закрывают строку — так однажды на раннере выполнился `/etc/init.d/rpcd restart`. Двойные кавычки.
- **Инструмент Bash коверкает обратные слэши в heredoc.** Для скриптов со спецсимволами писать отдельный файл, а не heredoc.
- **Модули с `export` не проходят `ucode -c -o /dev/null`** — это нормально, не считать ошибкой.
- **`mv` не отбирает файл у того, кто его пишет.** Ротация логов через `mv` положила роутер целиком (см. раздел 9). Процесс пишет по inode: после `mv` он продолжает писать в повёрнутый файл, а новый остаётся пустым навсегда. Только `cp` + обрезание на месте.
- **`/var/run` — это tmpfs, то есть оперативная память.** Всё, что там растёт без предела, съедает RAM, а не флеш. На 240 МБ это смертельно.
- **Имя службы в заголовке ошибки — не имя хоста.** `X-Cloudaicompanion-Trace-Id` увёл в сторону на два чата. Смотреть, куда клиент ходит на самом деле: `curl -s http://127.0.0.1:9090/connections` на роутере отдаёт `host`, `destinationIP` и `chains` по каждому живому соединению. Это единственный надёжный способ узнать домен и то, каким выходом он ушёл.
- **`domain_suffix` в sing-box сравнивает метки, а не строку.** `cloudcode-pa.googleapis.com` не покрывает `daily-cloudcode-pa.googleapis.com`.
- **На роутере нет `paste` и `pgrep`.** Скрипты разбора писать на `awk`, процессы искать через `ps w | grep`.
- **`scp` на роутер не работает** — нет `sftp-server`. Заливать файлы через `ssh root@… 'cat > /tmp/файл' < локальный`.
- **Перезапуск `rpcd` убивает все сессии LuCI.** Они живут в его памяти. После `/etc/init.d/rpcd restart` админка во всех браузерах отвечает `Access denied`, а страница висит на «Загрузка страницы…», не предлагая войти заново.
- **`GridSection` рисует ячейку через `textvalue()`**, то есть строкой. Узел, возвращённый из `cfgvalue()`, в DOM не попадает вообще — `id` на нём бесполезен. Адресоваться к ячейкам через `tr[data-sid]` и `[data-name]`, их таблица проставляет сама.
- **`<label>` пересылает клик на свой контрол.** Единственное место в LuCI, где `<select>` лежат внутри `<label>` — виджет частот Wi-Fi. Из-за этого один клик по кастомному триггеру давал два события, и обработчик «клик снаружи» закрывал список в тот же тик.
- **Версия пакета берётся из `Makefile`, а не из git-тега.** Релиз по тегу `v1.0.1` собрал файл `…-1.0.0-r20260810.apk`, и `apk` счёл его уже установленным. Поднимать `PKG_VERSION`/`PKG_RELEASE` вместе с тегом.
- **Порты Telegram для звонков не фиксированы.** Наблюдались `32000` и `32001` — ни один диапазон «проксировать звонки» их не покрывал. Перехватывать надо по **адресам** Telegram, тогда порт не важен. Правило маршрутизации при этом не нужно: адреса и так ведут в `main-out` через списки Re:filter, не хватало ровно перехвата.
- **`nc -z` в busybox не поддерживается**, а быстрый выход `nc` означает и «подключился», и «отказано». Проверять код возврата, и держать контрольный порт, про который заранее известно, что он открыт.
- **Проброс нельзя проверить с самого роутера по внешнему адресу** — правило отражения требует источник из LAN, а роутер ходит с WAN-адреса, и любой порт отвечает «refused». Нужна внешняя точка обзора.
- **Трафик на собственный адрес роутера нельзя пускать в прокси — иначе получается самоусиливающаяся петля.** Правило «проксировать звонки» сопоставляет только по порту, без оговорки про адрес назначения. Соединение на `<WAN-адрес>:5222` уходило через прокси, сервер честно подключался к запрошенному адресу — то есть обратно на `<WAN-адрес>:5222` — соединение приходило на WAN, снова попадало в редирект, снова матчилось тем же правилом. Каждый круг рождал новое соединение: замерено **769 одновременных**, 45% всей таблицы conntrack, и все они шли через туннель. Демонстрация экрана в Discord падала с ошибкой 2012, потому что поднималась в забитом под завязку туннеле. Две независимые дыры сложились в одну: `limcore_local_addr_v4` содержит только зарезервированные диапазоны и **публичный адрес роутера в него не входит**, а `limcore_redirect_lanac` выпускает свой guard по `iifname` только когда заданы интерфейсы прослушивания — с пустой настройкой WAN-входящие соединения свободно доходят до редиректа. Лечится строкой `fib daddr type local counter return` в начале `limcore_redirect` и `limcore_mangle_prerouting`: она ловит любой адрес, принадлежащий хосту, независимо от интерфейса, и продолжает работать при смене WAN-адреса — чего статический набор адресов не умеет. Симптом, по которому это узнаётся в логе ядра: повторяющиеся пары `inbound/redirect[redirect-in]: inbound connection to <WAN-адрес>:5222` и `outbound/vless[main-out]: outbound connection to <WAN-адрес>:5222`.

---

## 8. История коммитов

```
7e3b8a1  Say plainly that nothing works without the core
75687c6  Stop claiming success when the core was declined
88e88e3  Ask before installing the core instead of forcing it
d433916  Let Telegram calls through by address, not by port
1d0d23f  Send the Gemini phone app through WARP, not just the web one
f5e8db8  Fill in the delay column the node sweep was already measuring
03fbf17  Let the node check run when no node is selected
ac39ab6  Measure every node at once, not just the one in use
4982f2a  Record memory and tmpfs use with every health sample
dc481df  Stop log rotation from eating the router's memory
a876005  Bring the Gemini tile back to Google's app grid
c0fb972  Put the WARP probe on the Diagnostics page too
523a29a  Only install on a schedule when there is something newer
2cecafe  Keep LimCore updated out of the box
afdd23e  Send Antigravity's real backend through WARP
c58dfb2  Confirm a health-probe failure before recording it
80b41a3  Check every installed component, and record health continuously
813a549  Let WARP cover only the domains that need it
e689cfd  Say whether an update is needed, and keep logs long enough to read
cf1f121  Add Cloudflare WARP as a layer on top of the proxy
465647d  Revert the overlay threshold change — the measurement behind it was wrong
8113259  Name the signing key so apk can actually verify it
6f09441  Intercept Discord's RTC range too, not just its voice range
f37b2ea  Make "proxy calls" actually cover Discord voice
d37bb69  Stop the tproxy ip rule multiplying on every restart
d63d772  Don't fail an update because the core step hit a transient error
```

Рабочее дерево чистое, всё запушено в `main`.

---

## 9. Разбор отказа 12 августа 2026

Утром роутер перестал отвечать целиком: не открывалась даже админка по `192.168.1.1`, помогла только перезагрузка.

**Причина — наша регрессия в ротации логов** (коммит e689cfd, где обрезание заменили на `mv`).

`clean_log.sh` делал `mv core.log core.log.1` и создавал новый пустой `core.log`. Но sing-box держит лог открытым и пишет по inode: после `mv` он продолжал писать в `core.log.1`, а `core.log` навсегда оставался нулевым. Скрипт же измерял только `core.log` — то есть ротация не срабатывала больше никогда, и `core.log.1` рос без предела.

Подтверждение прямым наблюдением, а не рассуждением:

```
/proc/<pid sing-box>/fd/3 -> /tmp/run/limcore/core.log.1
core.log    0 байт (не менялся с момента ротации)
core.log.1  608853 → 613701 байт за 20 секунд
```

`/var/run` — это tmpfs, 117 МБ при 239 МБ памяти. Лог съел память, и роутеру стало нечем работать. Отсюда и симптом «недоступна админка»: это была не сеть, а нехватка RAM. По времени сходится: служба стартовала 11 августа около 15:03, отказ — около 09:00, то есть примерно 18 часов роста.

**Исправление:** `cp` + обрезание на месте — inode сохраняется, писатель остаётся направлен на файл, который мы можем очистить. Плюс предохранитель: если `*.log.1` перевалил за десятикратный порог, он обрезается на месте — это чинит уже сломанные экземпляры, где писатель застрял на повёрнутом файле, не дожидаясь перезапуска.

**Что усугубляло:** уровень логирования на роутере стоит `info`, а в нём пишется каждый DNS-запрос. По умолчанию в конфиге `warn`. Если скорость роста логов будет мешать — стоит вернуть `warn`, но это решение пользователя, а не обязательная часть исправления.

**Что сделано после разбора.** В `health_probe.sh` добавлены два поля: доступная память и занятое место в `/tmp`. Причина простая — во время отказа ни один из этих показателей никто не записывал, и диагноз пришлось восстанавливать по уже перезагруженному устройству. Теперь строка выглядит так:

```
2026-08-12 09:34:32 node=78ms wan=1.058ms dns=0ms/ok mem=84M tmp=2M
```

Там же `mv` заменён на `cp` + обрезание. Оговорка на будущее: в `health_probe.sh` `mv` работал правильно, потому что запись идёт через `>>`, а это переоткрывает файл на каждой строке. То есть это была не ошибка, а совпадение — и убрано именно поэтому, чтобы образец не пережил изменения способа записи.

`health.log` при этом ограничен и был ограничен всегда: своя ротация на 200 КБ плюс одно поколение, внутри самого `health_probe.sh`, а не в `clean_log.sh`.

---

## 10. Работы 13 августа

### Восстановлена потерянная фича

Проверка доступности узлов была собрана накануне **прямо на роутере и не попала в
git**. Расхождение нашлось сверкой контрольных сумм: четыре файла — режим `probe`
в `generate_client.uc`, методы `nodes_probe_*` в rpcd, кнопка и колонка в `node.js`,
поля `rss=`/`ct=` в `health_probe.sh`. Забрано в репозиторий, к нему три починки:

- Генератор выходил по `main_node = nil` **раньше** ветки `probe`, поэтому проверка
  не работала ровно тогда, когда нужна: узел умер, ищешь живой.
- Отсутствие метки завершения означало и «не запускалась», и «результаты уже
  забраны» — при неудачном старте кнопка крутилась вечно. Добавлена метка запуска.
- Колонка «Задержка» не заполнялась: `GridSection` рисует ячейку через
  `textvalue()`, и `<span>` с `id`, возвращённый из `cfgvalue()`, в DOM не попадал.

### Gemini на телефоне

Мобильное приложение **не ходит на `gemini.google.com`** — это веб-клиент. Оно
обращается к `robinfrontend-pa.googleapis.com`, которого в списке WARP не было, и
запрос уходил обычным прокси. Найдено по списку живых соединений с фильтром по
адресу телефона; в самой ошибке про регион этого хоста нет.

Само исправление вечером отменилось вместе с удалением WARP (раздел 4), но факт
про хост остаётся верным и пригодится, если тему будут поднимать заново.

### Звонки Telegram

Ключи согласовывались, дальше «Соединение…» навсегда. Сигнализация идёт по TCP и
проксировалась всегда (адреса Telegram есть в Re:filter), а медиа — UDP на
`91.108.9.x:32000–32001`, порты не входили ни в один диапазон «проксировать
звонки». Отправлено 152 пакета, получено 0. Лечится перехватом **по адресам**
Telegram в `firewall_post.ut`; после правки — 187 отправлено, 194 получено.

### Обложки Spotify

Музыка играла, картинки не грузились. Re:filter покрывает `spotify.com`, но не
`scdn.co` и `spotifycdn.com` — они уходили напрямую и упирались в таймаут. Два
ключевых слова в пользовательский список доменов (`proxy_list.txt`, правится в
интерфейсе). `i.scdn.co`: было `http=000` за 10 с, стало `403` за 0.36 с.

### Тема: выпадающие списки частот

Не открывались вовсе. Две причины, обе только у виджета частот Wi-Fi, потому что
это единственное место в LuCI, где `<select>` лежат внутри `<label>`. Подробности
в разделе «грабли». Выпущена `luci-theme-limcore` **1.0.1**.

### Установщик

Ядро больше не ставится безусловно — задаётся вопрос, как для Zapret и ByeDPI, по
умолчанию «да». При отказе служба не запускается и об этом честно сообщается.
Важно: отказ оставляет систему нерабочей **целиком**, см. незакрытую задачу 0.

### Не про LimCore, но всплыло

- RDP не подключался: на ПК не была включена служба после переустановки Windows.
  С роутера было видно, что 135 и 5357 отвечают, а 3389 и 445 молчат — то есть
  путь исправен, молчит служба. После включения на 3389 **сразу пришли боты**
  (`109.205.211.52`, `162.216.240.24`, установленные сессии). Внешний порт
  проброса сменён на `49731`; это отсекает массовые сканы, но не целенаправленный
  перебор — по-хорошему нужен доступ по списку адресов или через SSH-туннель.
